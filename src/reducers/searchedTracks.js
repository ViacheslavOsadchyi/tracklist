const initialState = {
    trackList: [],
    searchMask: '',
    loaded: false,
};

export default function searchedTracks (state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}