const initialState = {
    artistInfo: null,
    loaded: false,
};

export default function artist (state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}